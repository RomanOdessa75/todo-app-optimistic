/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: "export",
  basePath: "/todo-app-optimistic",
  assetPrefix: "/todo-app-optimistic/",
};

module.exports = nextConfig;
